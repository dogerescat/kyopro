#include <iostream>
#include <vector>
using namespace std;
using Graph = vector<vector<int>>;

int main() {
	int w, h; cin >> w >> h;
	Graph G(2*h+1, vector<int> (w+1, 1));
	for(int i = 1; i < 2*h; i++) for(int j = 1; j < w+1; j++) {

	}
}
